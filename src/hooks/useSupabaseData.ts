// Barrel file: re-exports all data hooks for backward compatibility.
// Individual hooks live in their own files for better separation of concerns.
export { useUsers } from './useUsers';
export { useClients } from './useClients';